# Just for learning

# Usage
[1] install requirements.txt  
[2] input your proxies (if any) in proxies.txt  
[3] input your JWT tokens in tokens.txt, inspect > Application > Local storage (choose datahive) > copy your JWT  
[4] setup acc available in config.json (leave it will default)  
[5] run by, py datahive.py  
